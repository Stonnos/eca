stages:
  - name: test
  - name: deploy

jobs:
  include:
    - stage: test
      script: mvn clean test
    - stage: deploy
      before_script:
        - cp settings.xml $HOME/.m2/
      script:
        - mvn clean deploy;
cache:
  directories:
    - $HOME/.m2